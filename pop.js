// Use the correct Array method to remove the last item of the fruits array.

const fruits = ["Banana", "Orange", "Apple"];
console.log(fruits.pop());
console.log(fruits);


// con el método pop elimina el último elemento y lo devuelve; si se vuelve a imprimir el array se verá que el elemento fué borrado.